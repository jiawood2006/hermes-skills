# 红线更正（请与 `read-first-current-routes.md` 一起读）

> 本文件建于 2026-09-17，用于纠正**旧文档里已被实测推翻的结论**。
> ⚠️ 后台整理进程当次无法改写 `read-first-current-routes.md` 与 `SKILL.md`（读校验/去重拦截），
> 所以更正落在这里；下次前台会话请把下面第 1 条合并进 `read-first-current-routes.md` §四 第 2 条。

## 1. ⚠️ 刀网事实更正（推翻 `read-first-current-routes.md` §四 第 2 条 & SKILL.md「产品特写」段）

| 项 | 旧文档写的 | **实测真机（以此为准）** |
|---|---|---|
| 刀网花纹 | 「浅金色**蜂窝编织**，不是光滑金盘/放射纹」 | **香槟金色「放射状网格」**——同心圆＋放射线交织成**菱形小格**，外圈一圈**黑色环带** |
| 机身 | 「黑色哑光缎面 / 深灰哑光」 | **枪灰／石墨灰「金属高光渐变」**（深灰底 + 竖向亮银反光），**不是哑光塑料、不是纯黑** |
| 长:径 | 1.9（对） | **1.93**（74×39mm）；官方 3/4 俯视角度会显短 |

依据：官方拉页图-7（刀网微距）、拉页图-12（尺寸图，亮光下机身呈银灰）、拉页图-0/3（整机全长）。
**危害**：按旧写法验收，正确渲染会被误判成错；写 prompt 时把刀网描述成"蜂窝"会直接画歪。

## 2. ⚠️ 视频里不要出现价格（2026-09-17 用户明令）

原话「视频里面不要体现价格，因为价格经常变化」。
做法：`overlay_cfg.json` 删掉 `price` 键（`assemble_r2v_overlays.py` / `assemble_v12.py` 会自动跳过），
15s 脚本里也要删 `ov_price.png`。尺寸重量类参数条（74×39mm / 70g / Type-C）可留。

## 3. 三条「写进 prompt 也压不住」的硬事实（必须靠素材/构图解决）

1. **参考人像会原样决定视频人物的胡子与表情**——候选图带小胡子/皱眉，视频里就是小胡子/皱眉，
   在视频 prompt 里写「上唇绝对没有胡须」「不要皱眉」**完全无效**（2026-09-17 实测白亏 4 轮）。
   → 人像候选必须写死：上唇完全光洁 + 下巴只有极淡青灰胡渣 + 眉毛放松不皱 + 看着镜头自然微笑 + 看起来年轻。
2. **机身会被拉长**——prompt 写「总长≈直径1.9倍」，模型仍按"电剃须刀=细长罐"画成 **2.56**（+35%）。
   → 三条同时上：写 **1.8** ＋说**短圆柱**＋**换机位**（别竖举到脸前，改斜握胸前、机头朝斜上方）。
3. **产品小到画面高 1/10 以下时机头糊成深色块**。
   → 给足尺寸（≥1/6 画面高）＋写「机头朝上、黑环＋金网清晰可辨、机身完整不糊」。

## 4. 分镜纪律：不能只罗列场景（2026-09-17 用户反馈）

原话「人物太呆滞，而且没有情景，单纯的罗列场景，能不能丰富一下」。
反例（我写出来的）：电梯镜只有「他走进电梯间，步伐自信，镜头跟随」。
正解：每个镜头给**动作链＋动机＋表情变化**——
「低头看手表 → 对着不锈钢门板上的倒影理衬衫领口 → 抬头，眼神从疲惫转为自信」。
自检：这个镜头主角**为什么**做这个动作？有没有前后两拍的变化？

## 5. 交付后用户报问题的标准动作

见 `references/user-reported-frame-triage.md` ＋ `scripts/locate_reported_frame.py`：
截图定位时间码 → 2 倍放大＋40px 网格量化 → 只重出受影响那段 → 走完整复核 → 飞书回读确认送达。
